doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title #{docTitle}
        link(rel="stylesheet", href="/css/main.css")
        //- may extend this -> adding block keyword + name
        block styles
    body 
        header.main-header
            nav.main-header-nav
                ul.main-header-item-list
                    li.main-header-item 
                        a(href="/", class=(path==='/' ? 'active': '')) Shop
                    li.main-header-item 
                        a(href="/admin/add-product", class=(path==='/admin/add-product' ? 'active': '')) Add product

        block content

//- define some placeholders/hooks in the layouts where other views can then enter their content
