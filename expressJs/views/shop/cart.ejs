<%- include('../includes/head.ejs') %>

</head>
<body>
    <%- include('../includes/navigation.ejs') %> 
    <main>
        <% if (products.length > 0) { %>
            <ul>
                <% products.forEach(prod => { %>
                    <li>
                        <%= prod.prodData.title %> (<%= prod.qty %>)
                    </li>
                <% }); %>
            </ul>
        <% } else { %>
            <h2>No Products in Cart!</h2>
        <% } %>
    </main>

</body>

<%- include('../includes/end.ejs') %>
