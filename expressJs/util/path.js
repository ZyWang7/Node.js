const path = require('path');

                            // give the path to he file that is responsible for tht fact that our application
module.exports = path.dirname(process.mainModule.filename);
                // get a path to that directory
